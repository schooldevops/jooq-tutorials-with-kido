package com.example.jooq;

/**
 * jOOQ Generator에 의해 자동 생성될 클래스의 모형상입니다.
 * 실제로는 build.gradle 설정에 의해 DB 스키마를 읽어 자동으로 `target/generated-sources` 하위에 생성됩니다.
 */
public class USERS {
    // 정상적인 컬럼명 상수
    public static final String EMAIL = "email";
    
    // 이 클래스에 E_MAIL 이라는 상수는 존재하지 않습니다.
}
